/**
 * Type that refers to `any`.
 *
 * Useful for bypass lint or type coverage checks.
 * Please use only in circumstances that require it.
 *
 * See {@link https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html#any *this link*}.
 *
 * @remarks Basically the same as TypeScript's built-in `any`.
 *
 * **It's better to not use `any`**.
 */
export type Any = any;

/**
 * Type that refers to `any`.
 *
 * Useful for bypass lint or type coverage checks.
 * Please use only in circumstances that require it.
 *
 * See {@link https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html#any *this link*}.
 *
 * @remarks Basically the same as TypeScript's built-in `any`.
 *
 * **It's better to not use `any`**.
 */
export type Anything = any;
