[typelab](../README.md) / [aliases](../aliases.md) / Void

```ts
type Void = void;
```

Type that refers to `void`.

Useful for bypass lint.

## Remarks

Basically the same as TypeScript's built-in `void`.

**It's better to not use `void` outside a return type**.
